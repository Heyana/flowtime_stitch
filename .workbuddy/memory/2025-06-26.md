# 2025-06-26

## Flowtime Stitch - style.md 设计对齐修复

对比 `style.md` 设计系统与代码实现，修复了 4 类偏差：

1. **阴影无定义** (P0): 在 `index.css` 新增 `@utility shadow-soft` / `@utility shadow-lifted`，对应 style.md 的 Level 1 (0 4px 20px rgba(0,0,0,0.04)) 和 Level 2 (0 8px 32px rgba(0,0,0,0.08))
2. **`card-shadow` → `shadow-soft`**: 全局替换所有组件中的 `card-shadow` 为 `shadow-soft`（4 个文件）
3. **Header 玻璃态**: `App.tsx` 去掉 `bg-background/80 backdrop-blur-md`，改为纯 `bg-background`，遵循 "solid opaque planes"
4. **卡片圆角统一**: `HistoryTab/BreaksTab/TimerTab` 中 `rounded-DEFAULT`(1rem) → `rounded-xl`(3rem)，匹配 Liquid Round 的极端圆润感

## Impeccable 设计审计修复 — 7 项闭环

基于 Impeccable critique 审计发现（总分 23/40），逐项修复：

### P0: BreathingModal 对齐 Light Solid
- 移除 `backdrop-blur-md` + 暗色覆盖层 → `bg-surface/95` 光固态
- 移除 `animate-ping` 波纹
- 白色文字 → 深色 `text-on-surface`
- 呼吸球体颜色改为半透明表面色 (`bg-secondary/15` 等)
- 完成提示移除 🌸 emoji

### P1: Inter 字体 → DM Sans
- `index.css`: `@import "@fontsource/dm-sans"` + `@import "@fontsource/sora"`
- `--font-sans: "Inter"` → `"DM Sans"`
- npm 安装 `@fontsource/dm-sans` `@fontsource/sora`

### P1: 卡片套卡片模式重构
- **History Stats**: 移除 `w-10 h-10 bg-primary/10 rounded-full` 图标圆形容器，改为内联 `flex items-center gap-2` 结构
- **Breaks 水追踪器**: 移除 `bg-secondary/10 rounded-full` 水滴图标容器，图标 + 标签内联
- **Breaks 拉伸区**: 移除 Accessibility 图标圆形容器，改为内联
- **Breaks 活动卡片**: Walk/Breathing 移除 `rounded-full` 图标容器，直接 `flex-shrink-0` 图标；移除 `animate-pulse`

### P2: 硬编码 '85%' → '--'
- `HistoryTab.tsx`: `efficiency > 0 ? efficiency% : '85%'` → `'--'`

### P2: Toggle 开关 ARIA 无障碍
- SettingsTab 3 个 toggle 全部添加 `role="switch"` `aria-checked` `aria-labelledby`
- 添加 `focus-visible:ring-2` 焦点可见样式

### P3: Emoji → 纯文案
- Settings toast: `'saved successfully! 🌸'` → `'saved successfully!'`
- Breaks water: `'🎉 Hydrated'` → `'Hydrated & Energized!'`
- Breaks stretch alert: `'Feel refreshed? 🌸'` → `'Feel refreshed?'`

### P3: 删除二次确认
- HistoryTab: 单条 session 删除新增内联确认态（Delete? → Check/X）
- Clear All 保留 `confirm()` 机制

**验证**: TypeScript 零错误，Vite build 通过，字体正常打包。

## PRD v1.0 编写

使用 prd-development skill（精简版），产出 `docs/prd/PRD-flowtime.md`：

- 10 个 User Stories（Timer 循环 / 任务标注 / 休息活动 / 历史统计 / 活动中心 / 呼吸引导 / 设置自动化 / Streak / 底部导航 / 数据持久化）
- 每个 Story 含验收标准 + 边界条件 + 代码依据（文件:行）
- Out of Scope 明确 8 项
- Dependencies 含版本号，风险含缓解
- 4 个 Open Questions（Lunch Break 未落地、时间输入类型、alert→Toast、每日目标）
- 用户流程图 (Mermaid) + 架构图 + 设计系统速查表

## Capacitor 原生集成（Android）

- 安装 `@capacitor/core` `@capacitor/cli` `@capacitor/app` `@capacitor/local-notifications` `@capacitor/haptics` `@capacitor/android`
- 新建 `src/capacitor-native.ts` 桥接模块：
  - `notifyTimerComplete(mode)` — 系统通知(响铃+弹窗)，Web 端降级 Web Audio
  - `hapticLight()` / `hapticHeavy()` — 触觉反馈
  - `saveBackgroundCheckpoint()` / `loadBackgroundCheckpoint()` / `calculateAdjustedTimeLeft()` — 后台时间追踪
  - `setupBackgroundListener()` — Capacitor App state 监听 + Web visibilitychange 兜底
  - 所有调用通过 `Capacitor.isNativePlatform()` 判断，动态 import 避免 web bundle 膨胀
- App.tsx 改造：
  - `triggerAlarm` 替换为 `notifyTimerComplete`
  - `startTimer` 加入 `hapticLight`
  - `handleTimerComplete` 加入 `hapticHeavy`
  - 新增 background tracking effect（visibilitychange + Capacitor App listener）
- 移除 Web Audio 内联代码 + 废弃的 Volume2 图标引用
- 添加 Android 平台（`npx cap add android`）
- Web build 增量仅 ~4KB（255.82KB gzip 75.47KB）
- TypeScript 零错误，Vite build 通过

## Android APK 构建

- 环境搭建：JDK 21 (Temurin) + Gradle 8.14.3 + Android SDK (platforms 34-36, build-tools 35/36/37)
- SDK 路径：`C:\Users\hzy\AppData\Local\Android\Sdk`
- Gradle 镜像：腾讯云 `mirrors.cloud.tencent.com` (wrapper + maven)
- BUILD SUCCESSFUL in 52s，产出 `android/app/build/outputs/apk/debug/app-debug.apk` (4.2MB)

## i18n + 双铃声 + Timer Bug 修复

- 新建 `src/i18n.ts`：中英文翻译表（~100 key），默认中文，Settings 语言切换
- **双铃声**：Work 完成 → 上行音阶 C5→E5→G5，Rest 完成 → 下行音阶 G4→E4→C4
- **Timer 闭包 bug 修复**：`handleTimerComplete` 改用 `useCallback` + `handleTimerCompleteRef` 避免 effect 中 stale closure 导致 Rest 倒计时不启动
- DebugTab：两个铃声独立测试按钮 + 弹窗 + 倒计时
- SettingsTab：新增语言切换（中文/English）
- BUILD SUCCESSFUL in 3s (Gradle daemon hot)

## 完整 i18n 收尾

- 补全 30+ 翻译 key：导航 tooltips(6)、拉伸动作名/描述(8)、默认值(8)、通知标题(2)、头像(3)、退出/重置 tooltip
- SettingsTab 全量重写：所有 label/description/placeholder 接入 t()
- BreaksTab 拉伸数据接入 t()，BreathingModal exit tooltip
- HistoryTab 默认值 'Coffee Break'/'Deep Work' → t('default.*')
- DebugTab 通知标题接入 t('notif.*')
- App.tsx 6 个底部导航 tooltip → t('tooltip.*')
- 总计 ~140 个 t() 调用覆盖全部 8 个文件

## 前台服务通知（@capawesome-team）

- 替换 `@capacitor/local-notifications` cancel+reschedule 方案
- 安装 `@capawesome-team/capacitor-android-foreground-service`，真正前台服务
- `updateForegroundService()` 原生更新，`silent: true` 静默
- 通知渠道 `Importance.Low`，自定义矢量图标 `ic_stat_flowtime.xml`
- AndroidManifest: FOREGROUND_SERVICE + WAKE_LOCK + POST_NOTIFICATIONS + service 声明
- 每 3 秒更新进度条 `████░░░░░░░░` + MM:SS

## 计时器 Bug 修复汇总

- **Stale closure**: `handleTimerComplete` 改用 `useCallback` + `handleTimerCompleteRef`
- **Updater 覆盖**: 独立 effect 检测 `shouldCompleteRef` 替代 updater 内直接调用
- **手动操作逻辑**: skipRest/endRestSession → 始终 `running`；auto-start 仅控制自动转换
- **完全停止按钮**: 新增 `stopAll()` + TimerTab 两处按钮，重置 idle focus
