# 2026-06-27

## 设计审计 + 改进 Spec

- 对 Flowtime 完整代码库执行设计审计（ui-ux-pro-max + impeccable）
- 评分 15/20 (Good)，发现 3 个 P1 + 5 个 P2 问题
- 关键发现：无暗色模式、弹跳缓动、无 reduced-motion、transition-all 反模式、SVG 硬编码颜色、Debug Tab 侵入导航、原生 confirm/alert、效率公式压制休息
- 产出 `docs/prd/SPEC-design-improvements.md` — 8 个 User Story 的改进规格文档

## 正面发现

- 设计 Token 系统完善（29 CSS props）
- 语义 HTML 良好（header/main/nav/section + role=switch + aria-checked）
- 双字体策略有意图（Sora 展示 + DM Sans 正文）
- 明确避免毛玻璃、Emoji 图标等 AI 反模式

## 设计改进执行（7/7 任务完成）

| # | 任务 | 变更文件 | 状态 |
|---|------|----------|------|
| D3 | prefers-reduced-motion + transition-all 修复 | index.css, TimerTab.tsx | ✅ |
| D2 | 弹跳缓动 → ease-emphasis (expo-out) | App.tsx, index.css | ✅ |
| D4 | SVG 颜色 Token 化（CSS 变量） | TimerTab.tsx | ✅ |
| D6 | ConfirmModal 组件 + 替换 confirm/alert | ConfirmModal.tsx(新), HistoryTab.tsx, BreaksTab.tsx | ✅ |
| D1 | 暗色模式支持（prefers-color-scheme） | index.css, index.html | ✅ |
| D5 | 导航收敛（5→4 Tab，Debug 移入 Settings） | App.tsx, SettingsTab.tsx, DebugTab.tsx | ✅ |
| D7 | 效率指标重构（% → 完成 Session 数） | HistoryTab.tsx | ✅ |

构建验证通过：`vite build` 2.41s，无错误。

## 暗色模式手动切换

- 新增 `src/theme.ts` — 主题状态模块（localStorage 持久化，`data-theme` 属性驱动）
- `index.css` 暗色 Token 重构为双入口：`[data-theme="dark"]` + `@media (prefers-color-scheme: dark)`（后者被 manual light/dark 屏蔽）
- Settings 页新增 3 选 1 主题开关：System / Light / Dark（Monitor/Sun/Moon 图标）
- `main.tsx` 启动时调用 `initTheme()` 恢复用户偏好

## 重构信号扫描 (refactoring-guide)

- 对 14 个源文件执行 20+ 信号检测，命中 10 个有效信号
- 6 高严重度：上帝组件(App.tsx 539行7类逻辑)、混合关注点(loadBackgroundCheckpoint读+删)、发散变更、错误边界缺失、时间耦合(双ref同步链)、位置错误业务逻辑
- 4 中严重度：数据团块、缺组合根、解析不验证(JSON.parse无校验)、非法状态
- 10 信号明确通过（无导入循环/无dumping-ground/无类型泄露/无布尔标志）
- 产物: `docs/refactoring-scan.md`

## 迁移 Spec：React → Flutter

- Grill-Me 会话完成 6 个决策分支：
  1. 设计系统 → Liquid Round 统一（Focus Red #b3272e / Rest Green #006d3e / Sora+Inter）
  2. 功能范围 → 全做（Timer 增强 + History 完善 + Breaks 整页 + Settings 补齐 + Debug + 通知/触觉/后台）
  3. 架构 → 保持现有分层，Breaks 拆 `presentation/breaks/`，4 tab 导航
  4. 平台 → 移动优先，加 Android
  5. 基础设施 → `flutter_local_notifications` 22 + 内置 HapticFeedback + WidgetsBindingObserver，排除 notification_master (v0.0.5 太新)
  6. AI → 跳过
- 两个子代理完整探索了两边代码库（React 14 文件/Flutter 全量分析）
- 产出 `docs/migration-spec.md`：设计 token 迁移表、目标文件结构、6 个 Phase 实现清单（0:设计系统 → 1:Timer → 2:History → 3:Breaks → 4:Settings → 5:Debug → 6:基础设施）
- 推荐 Skills 栈：frontend-design → impeccable → ui-ux-pro-max → improve-codebase-architecture

## Wind-Guide Skill

- 创建项目级 skill `D:\hxy\github\flowtime\.workbuddy\skills\wind-guide\`
- 内容覆盖：导入规范、别名表（17个）、cx() 条件 className、StyleProvider、4 类反模式（Spacer/嵌套flex/relative+Positioned/f-in-row）、滚动、颜色限制、背景透底、React→Wind 翻译对照表
- 触发条件：flowtime 项目中任何含 WDiv/className 布局的 .dart 文件
